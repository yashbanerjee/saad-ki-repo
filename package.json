{
  "name": "saad-ki-repo",
  "private": true,
  "description": "TaskFlow Enterprise monorepo — app lives in /workspace",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:app": "npm install --prefix workspace",
    "build": "node scripts/railway-dispatch.js build",
    "start": "node scripts/railway-dispatch.js start",
    "build:frontend": "node scripts/railway-dispatch.js build",
    "start:frontend": "node scripts/railway-dispatch.js start"
  }
}
