{
  "name": "saad-ki-repo",
  "private": true,
  "description": "TaskFlow Enterprise monorepo — app lives in /workspace",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix workspace && npm run prisma:generate --prefix workspace/backend && npm run build --prefix workspace/backend",
    "start": "node workspace/backend/scripts/start-railway.js",
    "build:frontend": "node workspace/frontend/railway-build.js",
    "start:frontend": "node workspace/frontend/railway-start.js"
  }
}
